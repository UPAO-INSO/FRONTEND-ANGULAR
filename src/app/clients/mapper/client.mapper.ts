import { Client, RESTClient } from '../interfaces/client.interface';

export class ClientMapper {
  // static mapRESTClientToClient(restClient: RESTClient): Client {
  // }
  // static mapRESTClientsToClientArray(restClients: RESTClient[]): Client[] {
  //   return restClients.map(ClientMapper.mapRESTClientToClient);
  // }
}
